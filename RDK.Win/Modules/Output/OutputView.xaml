<UserControl x:Class="RDK.Modules.Output.OutputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:control="clr-namespace:RDK.Windows.Controls"
             xmlns:data="clr-namespace:RDK.Windows.Data"
             xmlns:local="clr-namespace:RDK.Modules.Output"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <data:RichTextItemsToDocumentConverter x:Key="RichTextItemsToDocumentConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="Edit(_E)">
                <MenuItem Header="Clear(_C)" Command="{Binding ClearCommand}" />
            </MenuItem>
        </Menu>
        <ScrollViewer Grid.Row="1">
            <control:BindableRichTextBox IsReadOnly="True" Document="{Binding LogMessages, Converter={StaticResource RichTextItemsToDocumentConverter}}" />
        </ScrollViewer>
    </Grid>
</UserControl>
